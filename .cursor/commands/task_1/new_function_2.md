### 目前状态：
左右两部分

### 待开发功能：
左侧放置两个输入框，输入框下方放置一个‘登录’Button。
登录按钮的宽度要与输入框的宽度一致。
上方输入框内提示文字为‘用户名’，下方输入框内提示文字为‘密码’。
使用shadcn中的Button，Input组件。
都需要使用globals中的颜色。


- 整体的修改需要符合YAGNI原则
- 不要增加，不必要的新的功能
- 新增的代码要符合源代码的设计规则
- 只改代码，不写文档
- 不写测试脚本