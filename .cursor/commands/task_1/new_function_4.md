### 目前状态：
"Settings"页面

### 待开发功能：
‘Skills’改为和'System Prompt'文案一样的文字风格，并在前面加一个heroicons图标的Bolt。
删除之前skills的上传框，更改使用Grid。
每个Grid中，上面显示skills的名称，下面显示skills的描述。
每个Grid中，右上角增加一个开关的按钮，使用shadcn中的Switch组件。
只做UI，不需要具备业务逻辑。
先模拟3个skills的Grid。

- 整体的修改需要符合YAGNI原则
- 不要增加，不必要的新的功能
- 新增的代码要符合源代码的设计规则
- 只改代码，不写文档
- 不写测试脚本